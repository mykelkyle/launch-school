let foo = "bar";
console.log(foo);

foo;

// substring String (Instance)
// create Object (Static)
// fromCharCode String (Static)
// slice String,Array(Instance)
// toString String,Number,Array,Object (Instance)
