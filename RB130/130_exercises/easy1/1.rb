class Tree
  include Enumerable

  def each
    # To provide most of the functionality of the Enumerable module, all you have to do is define an `each` method that yields each member of the collection, one at a time
  end
end
